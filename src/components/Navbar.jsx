import './Navbar.css'

const Navbar = () => {
    return (<>
        <nav>
            <p >Home Page</p>
            <button id="registerButton">Sign Up</button>
        </nav>
        </>
    )
}

export default Navbar;